SET GLOBAL local_infile = 'ON';

CREATE USER IF NOT EXISTS 'dev'@'%' IDENTIFIED WITH mysql_native_password BY 'dev';
GRANT ALL PRIVILEGES ON *.* TO 'dev'@'%';

CREATE DATABASE IF NOT EXISTS DATASUS;
USE DATASUS;

CREATE TABLE IF NOT EXISTS `POBR` (
	`ANO_DIAGN` DECIMAL(38, 0) NOT NULL,
	`ANOMES_DIA` DECIMAL(38, 0) NOT NULL,
	`ANO_TRATAM` DECIMAL(38, 0),
	`ANOMES_TRA` DECIMAL(38, 0),
	`UF_RESID` DECIMAL(38, 0) NOT NULL,
	`MUN_RESID` DECIMAL(38, 0) NOT NULL,
	`UF_TRATAM` DECIMAL(38, 0),
	`MUN_TRATAM` DECIMAL(38, 0),
	`UF_DIAGN` DECIMAL(38, 0) NOT NULL,
	`MUN_DIAG` DECIMAL(38, 0) NOT NULL,
	`TRATAMENTO` DECIMAL(38, 0) NOT NULL,
	`DIAGNOSTIC` DECIMAL(38, 0) NOT NULL,
	`IDADE` DECIMAL(38, 0) NOT NULL,
	`SEXO` VARCHAR(1) NOT NULL,
	`ESTADIAM` DECIMAL(38, 0),
	`CNES_DIAG` DECIMAL(38, 0) NOT NULL,
	`CNES_TRAT` DECIMAL(38, 0),
	`TEMPO_TRAT` DECIMAL(38, 0),
	`CNS_PAC` BOOL,
	`DIAG_DETH` VARCHAR(3) NOT NULL,
	`DT_DIAG` VARCHAR(10) NOT NULL,
	`DT_TRAT` VARCHAR(10),
	`DT_NASC` VARCHAR(10) NOT NULL
);
